{
  "buildCommand": "pnpm turbo build --filter=@wapixia/dashboard...",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/dashboard/.next",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
